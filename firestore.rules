rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all operations during development and testing
    // TODO: Add proper authentication and security in production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
